# The nvidia RuntimeClass, using the real nvidia handler.
#
# Session pods reach the GPU through runtimeClassName: nvidia together with nvidia.com/gpu. Apply
# this once per cluster.
#
# The control plane checks that this RuntimeClass exists before marking a cluster connected.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia
